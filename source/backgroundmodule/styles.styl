/* = = = 
Variables... beware! this will become blended with all other modules on the same level
= = = */
// background = url("background-pattern.jpg") no-repeat center cover
background = rgb(0, 150, 169)

/* = = = 
Module Specific Styles
= = = */
#background-frame
    position fixed
    top 0
    left 0
    width 100%
    height 100vh
    z-index -1
    background background